<main class="app-shell">
  <section class="dashboard-shell">
    <header class="hero-card">
      <div class="hero-grid">
        <div>
          <p class="eyebrow">Symphony Admin</p>
          <h1 class="hero-title">New agent connection</h1>
          <p class="hero-copy">Create a managed agent connection.</p>
        </div>
      </div>
    </header>

    <%= render "form", agent_connection: @agent_connection, url: "/agent_connections", method: :post, submit_label: "Create agent connection" %>
  </section>
</main>
