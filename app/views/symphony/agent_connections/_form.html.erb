<section class="section-card">
  <% if agent_connection.errors.any? %>
    <div class="empty-state">
      <% agent_connection.errors.full_messages.each do |message| %>
        <p><%= message %></p>
      <% end %>
    </div>
  <% end %>

  <%= form_with model: agent_connection, url: url, method: method do |form| %>
    <div>
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>

    <div>
      <%= form.label :kind %>
      <%= form.text_field :kind %>
    </div>

    <div>
      <%= form.label :status %>
      <%= form.select :status, Symphony::AgentConnection::STATUSES %>
    </div>

    <div>
      <%= form.label :config_json, "Config (JSON)" %>
      <%= form.text_area :config_json, value: agent_connection.config.present? ? agent_connection.config.to_json : "" %>
    </div>

    <%= form.submit submit_label %>
  <% end %>
</section>
