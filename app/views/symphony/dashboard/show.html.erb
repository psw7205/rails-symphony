<% content_for(:title, "Symphony Dashboard") %>
<% content_for(:head) do %>
  <%= stylesheet_link_tag "symphony/dashboard", "data-turbo-track": "reload" %>
<% end %>

<main class="app-shell" data-controller="dashboard" data-dashboard-interval-value="5000">
  <section class="dashboard-shell">
    <header class="hero-card">
      <div class="hero-grid">
        <div>
          <p class="eyebrow">Symphony Observability</p>
          <h1 class="hero-title">Operations Dashboard</h1>
          <p class="hero-copy">
            Current state, retry pressure, token usage, and orchestration health for the active Symphony runtime.
          </p>
        </div>
        <div class="status-stack">
          <span class="status-badge status-badge-live">
            <span class="status-badge-dot"></span>
            Live
          </span>
        </div>
      </div>
    </header>

    <section class="metric-grid">
      <article class="metric-card">
        <p class="metric-label">Running</p>
        <p class="metric-value numeric"><%= @snapshot[:counts][:running] %></p>
        <p class="metric-detail">Active issue sessions in the current runtime.</p>
      </article>

      <article class="metric-card">
        <p class="metric-label">Retrying</p>
        <p class="metric-value numeric"><%= @snapshot[:counts][:retrying] %></p>
        <p class="metric-detail">Issues waiting for the next retry window.</p>
      </article>

      <article class="metric-card">
        <p class="metric-label">Total tokens</p>
        <p class="metric-value numeric"><%= number_with_delimiter(@snapshot[:codex_totals][:total_tokens]) %></p>
        <p class="metric-detail numeric">
          In <%= number_with_delimiter(@snapshot[:codex_totals][:input_tokens]) %> /
          Out <%= number_with_delimiter(@snapshot[:codex_totals][:output_tokens]) %>
        </p>
      </article>

      <article class="metric-card">
        <p class="metric-label">Runtime</p>
        <p class="metric-value numeric"><%= format_runtime(@snapshot[:codex_totals][:seconds_running]) %></p>
        <p class="metric-detail">Total Codex runtime across completed and active sessions.</p>
      </article>
    </section>

    <section class="section-card">
      <div class="section-header">
        <div>
          <h2 class="section-title">Rate limits</h2>
          <p class="section-copy">Latest upstream rate-limit snapshot, when available.</p>
        </div>
      </div>
      <pre class="code-panel"><%= @snapshot[:rate_limits]&.to_json || "n/a" %></pre>
    </section>

    <section class="section-card">
      <div class="section-header">
        <div>
          <h2 class="section-title">Running sessions</h2>
          <p class="section-copy">Active issues, last known agent activity, and token usage.</p>
        </div>
      </div>

      <% if @snapshot[:running].empty? %>
        <p class="empty-state">No active sessions.</p>
      <% else %>
        <div class="table-wrap">
          <table class="data-table data-table-running">
            <thead>
              <tr>
                <th>Issue</th>
                <th>State</th>
                <th>Runtime</th>
                <th>Last event</th>
              </tr>
            </thead>
            <tbody>
              <% @snapshot[:running].each do |entry| %>
                <tr>
                  <td>
                    <div class="issue-stack">
                      <span class="issue-id"><%= entry[:issue_identifier] %></span>
                      <%= link_to "JSON", api_v1_issue_path(entry[:issue_identifier]), class: "issue-link" %>
                    </div>
                  </td>
                  <td><span class="state-badge <%= state_badge_class(entry[:state]) %>"><%= entry[:state] %></span></td>
                  <td class="numeric"><%= format_runtime(entry[:elapsed_seconds]) %></td>
                  <td><%= entry[:last_codex_event] || "n/a" %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% end %>
    </section>

    <section class="section-card">
      <div class="section-header">
        <div>
          <h2 class="section-title">Retry queue</h2>
          <p class="section-copy">Issues waiting for the next retry window.</p>
        </div>
      </div>

      <% if @snapshot[:retrying].empty? %>
        <p class="empty-state">No issues are currently backing off.</p>
      <% else %>
        <div class="table-wrap">
          <table class="data-table">
            <thead>
              <tr>
                <th>Issue</th>
                <th>Attempt</th>
                <th>Due at</th>
                <th>Error</th>
              </tr>
            </thead>
            <tbody>
              <% @snapshot[:retrying].each do |entry| %>
                <tr>
                  <td>
                    <div class="issue-stack">
                      <span class="issue-id"><%= entry[:issue_identifier] %></span>
                      <%= link_to "JSON", api_v1_issue_path(entry[:issue_identifier]), class: "issue-link" %>
                    </div>
                  </td>
                  <td><%= entry[:attempt] %></td>
                  <td class="mono"><%= entry[:due_at] || "n/a" %></td>
                  <td><%= entry[:error] || "n/a" %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% end %>
    </section>
  </section>
</main>
